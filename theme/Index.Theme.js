import Color from "../constants/Color";

export const theme = {
    backgroundColor: Color.Putih,
    text: Color.Hijau
}

export const style = {
    backgroundColor: theme.backgroundColor,
    text: theme.text
}