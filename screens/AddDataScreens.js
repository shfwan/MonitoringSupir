import { View, Text, SafeAreaView } from 'react-native'
import React from 'react'
import { StatusBar } from 'expo-status-bar'
import { COLORS } from '../constants/Color'

const AddDataScreens = () => {
  // return (
    
  // )
}

export default AddDataScreens